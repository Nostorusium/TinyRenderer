# TinyRenderer 项目协作约定

本文档记录已经达成的意图、规格与工程约定，供后续开发者和 Agent 直接继承。不要在没有明确理由和验证证据时悄悄改变这些约定；若确需修改，应同步更新本文档、相关测试和示例图像。

## 1. 项目意图

本项目用于通过亲手构造一条 CPU 光栅化流水线学习计算机图形学。首要目标是理解每个阶段解决的问题、数学含义和失败模式，而不只是尽快得到一张看起来正确的图片。

实现应当：

- 可运行、可观察、可分阶段验证。
- 优先清晰度、可解释性和可修改性，而非性能或通用性。
- 让核心算法能够被单独测试和讲解。
- 保留足够证据，区分“代码能运行”“行为符合规格”和“确实达成学习目标”。

### 1.1 已确认的学习协作方式

- 使用 C++20 实现。
- 使用 CMake 作为构建系统。
- 使用 Visual Studio Code 作为主要代码编辑器和调试界面；不要要求用户改用完整 Visual Studio IDE。
- Windows 上可使用独立的 Visual Studio Build Tools 提供 MSVC、Windows SDK 和 CMake 支持，编辑器与编译工具链应保持解耦。
- 源文件统一使用 UTF-8。MSVC 必须启用 `/utf-8`，让中文教学注释既能保留，也能稳定编译。
- Agent 主导编写、运行和验证代码；用户主要通过阅读实现、观察结果和提问来学习。
- 每次只推进一个较小且可运行的增量，避免一次生成大量难以审查的代码。
- 用户没有 C++ 使用经验，不要求其从空白处独立写出核心算法；适合手敲的短小完整示范可以作为可选练习。
- 用户患有 ADHD，偏好通俗、直接、短小的中文；默认只给一至三个注意力重点，更多内容在追问时展开。
- 用户会用自己的语言在代码中留下少量理解性注释。把这些注释视为风格和学习证据，保留其个人措辞，只纠正事实错误或潜在误导。
- 向量、矩阵、重心坐标、深度测试、插值和裁剪等核心图形学算法需要明确标为重点；样板代码和构建细节降低教学优先级。
- 新增或改变生成物时使用 `$generative-software-engineering`。每个实现增量完成后的正式教学交付，或用户明确要求系统性复盘时，使用 `$code-to-concepts`；临时理解确认、术语追问和简短纠正直接回答，不重新加载 Skill。通用教学方法由 Skill 维护，不在本文件重复。

## 2. 当前范围

推荐的第一条路线是离线、CPU、单线程光栅化器：读取简化模型数据，在内存 framebuffer 中渲染，输出 PPM 或 TGA 图片。

第一版非目标：

- 不使用 OpenGL、Direct3D、Vulkan 等 API 的光栅化能力。
- 不做实时编辑器、游戏引擎或通用场景系统。
- 不追求完整 OBJ/MTL 标准支持。
- 不做骨骼动画、PBR、全局光照、多线程或 SIMD 优化。
- 不提前引入 ECS、复杂资源系统或无法由当前需求证明必要的抽象。

## 3. 已确认的数学与坐标约定

### 3.1 三维空间

- 使用右手坐标系。
- `+X` 向右，`+Y` 向上，`+Z` 从屏幕指向观察者。
- 摄像机在观察空间中默认朝 `-Z` 方向看。
- 叉积满足 `X × Y = Z`。

### 3.2 向量与矩阵运算

- 使用列向量。
- 矩阵左乘向量：`v' = M * v`。
- 局部坐标到裁剪坐标的组合为：

  `v_clip = Projection * View * Model * v_local`

- 变换从右向左执行。
- 仿射变换的平移分量位于齐次矩阵最后一列。
- 矩阵元素通过 `(row, column)` 的语义访问。

### 3.3 矩阵存储

- 自有矩阵类型采用 row-major 连续存储，主要方便逐行查看和调试。
- row-major 只是内存布局，不改变列向量、左乘或组合顺序。
- 算法不得依赖裸内存顺序表达数学含义。
- 与外部库交换矩阵时必须通过明确的转换边界，不得依靠隐式转置或偶然兼容。

### 3.4 裁剪空间、NDC 与深度

- 透视除法前使用齐次裁剪坐标。
- 透视除法为：`v_ndc = v_clip.xyz / v_clip.w`。
- NDC 的 `x`、`y`、`z` 范围采用 `[-1, 1]`。
- 视口变换将 NDC 深度映射到 `[0, 1]`：`depth = z_ndc * 0.5 + 0.5`。
- 深度值越小表示越靠近摄像机。
- 深度缓冲建议初始化为正无穷，新深度严格小于旧深度时通过测试。

### 3.5 屏幕与图片坐标

- 图片原点位于左上角。
- 屏幕 `+X` 向右，`+Y` 向下。
- NDC 到屏幕坐标时只在视口变换中翻转 Y：

  `screen_x = (ndc_x + 1) * width / 2`

  `screen_y = (1 - ndc_y) * height / 2`

- Y 翻转只是三维空间到二维图片的映射，不改变三维世界的手性。
- 采样位置使用像素中心 `(x + 0.5, y + 0.5)`。

### 3.6 三角形绕序

- 在视口 Y 翻转之前，以逆时针（CCW）为正面。
- 基础光栅化稳定前不启用背面剔除。
- 加入背面剔除时，必须明确在哪个空间判断绕序，并考虑视口 Y 翻转会反转二维绕序。
- 三角形共享边最终应采用一致的边界规则，例如 top-left rule，避免裂缝和重复覆盖。

## 4. 推荐的渐进式里程碑

每个阶段都应形成一个可运行、可观察、可撤销的小增量：

1. framebuffer、颜色、单像素写入和 PPM/TGA 输出。
2. 线段绘制与基本边界处理。
3. 简化 OBJ 顶点和面解析，输出线框模型。
4. 基于包围盒和重心坐标的三角形填充。
5. 深度缓冲和与绘制顺序无关的遮挡。
6. Model、View、Projection、Viewport 变换流水线。
7. 顶点法线和 Lambert 漫反射。
8. UV、纹理采样与属性插值。
9. 基于 `1/w` 的透视校正插值。
10. 近裁剪面、视锥裁剪、退化三角形与数值健壮性。
11. 在基础稳定后，再评估 shader 抽象、法线贴图和阴影等扩展。

不要在早期一次性设计完整渲染框架。只在重复与变化已经被观察到之后提炼接口。

## 5. 推荐的职责边界

- 数学层：向量、矩阵、齐次坐标和变换。
- 图像层：颜色、像素缓冲、边界检查和图像读写。
- 模型层：顶点、面、UV、法线和有限格式解析。
- 光栅化层：覆盖判断、重心坐标、属性插值和深度测试。
- 着色层：在基础阶段稳定后，从光栅化流程中逐步分离。
- 应用层：组织输入、相机、渲染步骤和输出，不承载底层算法。

图像层不应理解三角形；模型加载器不负责投影和绘制；光栅化器不直接解析 OBJ。

## 6. 验证协议

### 6.1 实现正确性

- 对向量、矩阵、点积、叉积、归一化和变换组合做确定性测试。
- 检查重心坐标之和接近 1，并覆盖边、顶点和退化三角形。
- 检查 framebuffer、深度和纹理访问不越界。
- 检查固定输入不会产生意外的 NaN 或无穷值。

### 6.2 需求符合性

- 保存小型确定性场景：单三角形、共享边三角形、重叠三角形和倾斜纹理平面。
- 使用像素级或带容差的图像比较，不以“程序成功退出”代替渲染验证。
- 验证改变绘制顺序不会改变深度遮挡结果。
- 将仿射插值与透视校正结果并排比较。
- 坐标约定相关测试应包含已知点经过 Model、View、Projection 和 Viewport 后的预期坐标。

### 6.3 意图对齐

每完成一个阶段，应能解释：

- 该阶段解决了流水线中的什么问题。
- 核心公式的几何含义是什么。
- 改变某项约定会造成什么可预测的画面变化。
- 当前图片正确性的证据是什么，还有什么尚未被证明。

## 7. 外部库兼容边界

不要用“row-major/column-major”代替完整约定。接入库时分别核对：向量方向、乘法侧、连续存储、手性、相机朝向和 NDC 深度范围。

- GLM/OpenGL：通常与右手系、列向量、`P * V * M * v` 和 `[-1, 1]` 深度一致；裸内存通常按列布局，交换时需转换。
- Eigen：支持列向量，并允许显式选择 `RowMajor`，可与本项目直接对齐；手性和投影由调用方定义。
- nalgebra：列向量、列主序，并提供右手/左手观察变换；使用裸数组时需处理存储布局。
- DirectXMath：通常使用行向量、row-major 和 `v * M`；接入时需转置矩阵、反向理解组合书写顺序，并核对 `[0, 1]` 深度。
- 模型与纹理导入：在单一导入边界处理上轴、前轴、绕序和 UV 垂直方向，不要把修正散落到渲染流程中。

为外部数据或库编写显式适配函数，并用已知点、已知矩阵和小型图像验证转换结果。

## 8. 尚未确认的高影响选择

以下内容仍是候选，不得视为已确认需求：

- 第一版最终停在哪个里程碑，以及纹理、裁剪之后是否继续扩展。

更远范围应依据阶段性结果逐步决定。

## 9. 当前进度

- 已完成第一个可运行增量：`Color`、CPU framebuffer、安全像素读写和二进制 PPM（P6）输出。
- `image_tests` 已验证尺寸、像素索引、越界拒绝、PPM 文件头和每像素三字节布局。
- 已完成第二个可运行增量：用整数误差累积绘制任意方向线段，并将结果写入既有 framebuffer。
- 当前示例生成五条不同方向和边界情况的线段：`output/lines.ppm`。
- `rasterizer_tests` 已覆盖水平、垂直、陡斜率、负斜率、单点、部分越界和端点反转。
- 已完成第三个可运行增量：读取简化 OBJ 的顶点与三角形面，并用现有线段函数输出线框模型。
- 当前 OBJ 边界：支持正数顶点索引和 `v/vt/vn` 形式，只读取第一个顶点索引；不支持多边形面和负索引。
- 当前示例读取 `models/cube.obj`，生成 `output/wireframe.ppm`。
- `model_tests` 已覆盖顶点、三角形面、OBJ 到 C++ 的索引转换、斜杠索引、非三角形和越界索引。
- 已完成第四个可运行增量：在屏幕空间内用包围盒限制扫描范围，并在像素中心计算重心坐标来填充三角形。
- `rasterizer_tests` 已验证顶点与边上的重心坐标、权重和为 1、两种绕序、退化三角形和部分越界三角形。
- 当前示例生成单个填充三角形：`output/triangle.ppm`。边界暂时全部包含，统一的 top-left rule 留到共享边规则增量处理。
- 下一候选增量是深度缓冲和与绘制顺序无关的遮挡；线框阶段的临时斜投影之后再由矩阵变换替代。
